export default ({ router }) => {
  // router.beforeEach((to, from, next) => {
  //   console.log('切换路由', to.fullPath, from.fullPath)

  //   // 触发 google 的 pv 统计
  //   if (typeof gtag != "undefined") {
  //     gtag('config', 'G-FR9MQET06Y', {'page_path': '/new-page.html'})
  //   }
  //   // continue
  //   next()
  // })
}
